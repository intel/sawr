/////////
// Depth Enhance configuration file
// Minimalistic
/////////

name = "Minimalistic";

filters =
(
    //////////
    // HOLE FILLING
    //////////
    {
        name       = "HoleFilling"; 
        parameters = (
            { name    = "radius";
              default = 5;
              max     = 9; }
        );
    },

    //////////
    // RENORMALIZE
    //////////
    {
        name       = "Renormalize"; 
        parameters = (
            // Don't display the following parameter in the interface,
            // just set its value
            { name    = "crop";
              default = 2500;
              control = FALSE; }
        );
    }
)
